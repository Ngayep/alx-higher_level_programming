#!/usr/bin/python3
"""A function that adds two integers"""


def add_integer(a, b=98):
    """checks if the digits to add are integers
       and raises an exception if they are not
        args: a- an integer
              b- 98
              if they are floats, they should be casted as integers
    """

    if a isinstance is
